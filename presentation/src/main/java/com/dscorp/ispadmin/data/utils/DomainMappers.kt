package com.dscorp.ispadmin.data.utils

import com.dscorp.ispadmin.domain.model.Payment

/**
 * Extiende Payment para convertirlo al modelo de dominio
 * En una implementación completa, tendrías que mapear todos los campos necesarios
 */
fun Payment.mapToDomain(): com.dscorp.ispadmin.domain.model.Payment {
    return this
} 